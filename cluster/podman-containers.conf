[containers]
# Use k8s-file log driver instead of journald. When podman uses journald as
# the log driver but the events backend is "file" (common in CI), the
# "podman logs --follow" command is not supported. Kind relies on log following
# during cluster creation, so we must use a compatible log driver.
log_driver = "k8s-file"
